# Runbook

## Start

```powershell
cd "C:\Users\Admin\Documents\warriorplus MMO\Agent PLR Saas"
pip install -r requirements.txt
python app.py
```

## Daily Workflow

1. Put legal PLR files into `plr_files/`.
2. Run `1. Scan PLR folder`.
3. Run `3. Analyze all products`.
4. Run `4. Generate 10 new product ideas`.
5. Run `13. Export full product pack`.
6. Review files in `exports/`.

## Best First Offer

Start with `AI PLR Rebrand Kit`.

Use the SaaS plan only after the digital product gets buyer interest.
