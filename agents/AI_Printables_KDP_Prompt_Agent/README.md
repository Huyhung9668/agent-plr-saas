# AI Printables KDP Prompt Agent

This agent compresses key knowledge from `G:\Documents\warriorplus MMO\Agent PLR Saas\input_files\AI Printables KDP Promt Skill` into a fast brain + skill system.

## How To Call
Use tags such as `#ai-printables #kdp #market-pattern` or `#ai-printables #deep-file-writer`.

## How To Use Skills
Load `routing/tag_router.json` or `skills/_index.json`, then open only the matched skill file.

## How To Use Brain
Start with `brain/AI_PRINTABLES_KDP_BRAIN.json` for fast routing and `brain/AI_PRINTABLES_KDP_BRAIN.md` for detailed rules. Load specialist brain docs only when needed.

## How To Update Skill
When feedback says missing/wrong/fix/ch?m k? h?n, patch the relevant skill, increase version, and log changes in `skills/_changelog.md`, `skills/_usage_log.md`, and `memory/user_feedback_log.md`.

## Product Pack Workflow
Market pattern ? competitor matrix ? offer gap ? product blueprint ? deep file writer ? prompt/buyer tests ? risk/compliance audits ? sales/listing/JV/delivery ? export ZIP ? public launch audit.

## Test Prompt
`#ai-printables #kdp #market-pattern`

## Export ZIP
Use `#export-zip` only after real product files exist.

## Backup Before Cleanup
Do not delete input source files until brain has been tested, output compared to source, and a backup ZIP exists.
